<UserControl
    x:Class="QRBarcode.Controls.AboutQR"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QRBarcode.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Source={StaticResource UIStrings}, Path=[AboutDetails]}" TextWrapping="Wrap" TextAlignment="Justify" Margin="5" FontSize="15"/>
        <TextBlock Grid.Row="1" Text="{Binding Source={StaticResource UIStrings}, Path=[AboutDetails2]}" TextWrapping="Wrap" TextAlignment="Justify" Margin="5" FontSize="15"/>
    </Grid>
</UserControl>
